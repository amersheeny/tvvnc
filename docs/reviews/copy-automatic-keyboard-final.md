REVIEW-ID: 4511adc8-c8a2-4a11-908c-14248e81f535
DATE: 2026-09-22
REVIEWER: content-strategist (Claude subscription CLI, cross-model)
ARTIFACT: /Users/amsh/worktrees/tvVNC/worktree/docs/reviews/copy-automatic-keyboard-final.md
DESCRIPTOR: none — derived from /Users/amsh/worktrees/tvVNC/worktree/assets/copy/en.json and supplied existing renders

STRING: replaceTvFieldAction
KEYS: replaceTvFieldAction
ENGLISH: 'Replace'
VERDICT: APPROVE
REASONING: This closes the gap exactly where it was open. The dialog asks "Replace the TV's text?" and the affirmative control now answers that question with its own verb instead of answering a different one, which is the whole point of a confirmation: a user who reads nothing but the two buttons still knows they are authorising a replacement rather than an ordinary send. Keeping the bare verb is right, because the title already carries the object and a two-button row beside Cancel reads fastest when the affirmative is a single word; Android's own destructive confirmations are built the same way. The change of verb between the button the user pressed, which stays Send, and the button that confirms, which is now Replace, is informative rather than confusing, since it is the signal that this particular send is not the ordinary one. The word also appears in the title, the body and the button, and that triple repetition is a virtue in a surface this small: there is no way to misread which action is being confirmed. Nothing in the label promises the write will succeed, which matters because the dialog cancels itself if the target, the field revision or the value moves while it is open.
RISKS: The dialog is the only thing distinguishing the two outcomes of the same Send button, since a phone-only draft inserts at the television's cursor with no dialog at all while this path replaces the field; the copy is correct precisely because the warning appears only when the behaviour turns destructive, so the dialog must never be suppressed or remembered-dismissed for this state. If the dialog is ever reused for an insert-style send, "Replace" becomes a false statement and both it and the title must change together. Nothing here offers an undo, and none is claimed, so this dialog is the user's only protection.

STRING: privateDraft
KEYS: privateDraft
ENGLISH: 'Text you hide is cleared when you leave the Keyboard or switch TVs, even if you show it again.'
VERDICT: APPROVE
REASONING: This fixes the orphaned term properly rather than papering over it. "Text you hide" points at a control the user can actually see and press, the eye labelled "Hide text", so the sentence is traceable to something on screen in a way that "Private text" stopped being once the chip was removed; ordinary cause and effect replaces a name that no longer names anything. The closing clause does the second job, which is explaining why this line is still showing after the user has revealed the text: "even if you show it again" uses the other eye label's own verb and turns what would otherwise look like a stale warning into the most useful part of the sentence. Putting that clause last is the right order, because it is the surprising half and it lands where it will be remembered. The claim stays inside what the app can honestly promise: it describes only when the app's own held text is discarded, and says nothing about what a keyboard learned before hiding began and nothing about third-party keyboards, so nothing here can be read as a guarantee the product cannot keep. Register and capitalisation match the surrounding screen, with the uncontracted verb used by the other messages in this region and "the Keyboard" capitalised as the destination is elsewhere. The growth from sixty-four to ninety-three characters buys both repairs, and you have already said length is not a constraint.
RISKS: "Cleared" borrows the verb of the Clear button sitting on the same screen; the passive construction and the explicit timing clause keep them apart, and the word carries over from the shipped line, so this is a watch item on the rendered screen rather than a change to make. The sentence is scoped to hidden text and says nothing about what happens to ordinary text on leaving, which is correct for a line that only appears while the buffer is sensitive but means it must not be reused as a general helper. It has not been rendered, so its wrap at the largest text size, where it will run to several lines, is unseen.

BRIEF DEFECT: None open. Both neighbour issues raised in the previous round are resolved with real strings rather than footnotes: the affirmative button now answers its own dialog's question, and the privacy helper now names the controls it depends on and explains its own persistence after revealing. One deliberate omission is recorded rather than re-raised: the product still says nothing anywhere about the non-learning input configuration or the cleared local draft cache, and on your instruction the helper does not mention them. That silence is the right call on the evidence available, because the app cannot speak for a third-party keyboard and cannot undo what one learned before hiding started, so any wording covering it would be a promise the product is not in a position to keep. It is noted here only so that the absence stays a decision on the record rather than an oversight discovered later.

COPY REVIEW COMPLETE: 2 strings (2 approved, 0 reworded)
