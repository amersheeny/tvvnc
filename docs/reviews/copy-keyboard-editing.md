REVIEW-ID: 4511adc8-c8a2-4a11-908c-14248e81f535
DATE: 2026-09-21
REVIEWER: content-strategist (Claude subscription CLI, cross-model)
ARTIFACT: /Users/amsh/worktrees/tvVNC/worktree/docs/reviews/copy-keyboard-editing.md
DESCRIPTOR: none — derived from assets/copy/en.json and supplied app screenshots

STRING: editingKeys
KEYS: editingKeys
ENGLISH: "Editing keys"
VERDICT: APPROVE
REASONING: The verdict is unchanged and is now resting on the exact strings rather than on an inferred pattern, and one premise inside the earlier record was wrong and is corrected here. I previously described the product's television marker as a leading "TV" placed at the front of labels, and cited "TV Backspace" as evidence; that label is not shipped and the citation was wrong. The real system, read from the exact strings you have supplied, is positional by element type: the section heading carries the marker in front, as "TV editing keys", while every control carries it behind, as "Backspace on TV", "Delete on TV", "Move cursor left on TV", "Move cursor right on TV" and "Enter on TV". That is a more disciplined convention than the one I described, because it lets each control lead with its action and qualify its target afterwards, which is the right order for both scanning and speech. The correction does not disturb this heading, because a heading is the one element that does use the leading form, so removing "TV" from "TV editing keys" to produce "Editing keys" is exactly the parallel operation for this element type. The substantive reason for approving stands untouched and is now better supported: the section spans two modes with different reach, since in active Live edit the keys change the mirrored field and that change travels to the television, while in the paused state, which you have now confirmed disables Send and shows the warning, nothing travels at all. A heading that claimed the television would be false in the paused state and a heading that claimed local-only would be false in the live state, so the neutral form is the only one true in both.
RISKS: The difference between "TV editing keys" and "Editing keys" remains a single deleted word, and a user switching modes is unlikely to register it. That is tolerable only because the body underneath changes from one short shipped sentence to a visibly different block; if the implementation ever makes the two bodies look alike, the heading stops carrying any differentiating load and should return for review. The phone's own typing keyboard sits directly below this section in the supplied dark screenshot at /Users/amsh/worktrees/tvVNC/maestro-review-screens/2026-09-21_012940/screens/takeScreenshot/private-keyboard-dark.png, so a heading without any qualifier could in principle be read as labelling those keys; the shipped heading has the same exposure and a divider separates the section, so this is a watch item for the post-build review on the rendered screen rather than a change to make now.

STRING: liveEditingBody
KEYS: liveEditingBody
ENGLISH: "Edits update the TV field. Enter acts on the TV."
VERDICT: REWORD
REWRITE: These keys change the text above and on the TV. Enter sends your text, then presses Enter on the TV.
REASONING: The verdict, the rewrite and the reasoning all stand, and the exact Compose strings you have supplied strengthen two specific choices inside the replacement that were previously judged from a screenshot alone. The first sentence is a deliberate reversal of the shipped Compose sentence "These keys act on the TV, not the draft above.", which is now confirmed word for word; a user who learned that sentence has been told the keys do not touch the visible text, and in Live edit that is no longer true, so leaving the reversal unstated lets a false rule stand and guarantees surprise the first time Backspace changes something the user can see. Note what the replacement deliberately does not borrow: the shipped sentence calls the visible content "the draft", and that noun is correct only in Compose, because in Live edit the field holds the mirrored television text rather than a draft the user is composing privately. Reusing "draft" here would have been a factual error about what the field contains, so the replacement keeps the shipped spatial word "above" and drops the mode-specific noun, which is precisely the right split. The second sentence replaces a restatement of the button's own label with the part of the button's behaviour that cannot be seen. With the exact control label now confirmed as "Enter on TV", the submitted sentence "Enter acts on the TV." is revealed to be an even closer echo of the button than I could establish before, since the label already contains both the word "Enter" and the words "on TV"; spending half of a two-sentence explanation repeating a label the user can read a few millimetres away, while omitting that this key first flushes the latest text and only then sends the television's Enter, is the defect. The replacement's phrasing "presses Enter on the TV" now maps onto the confirmed label almost exactly, with only the definite article added as ordinary prose requires, so the sentence names the button the user must press.
RISKS: At one hundred characters the replacement is about twice the length of the shipped Compose body it sits opposite, and judged against the large-text capture at /Users/amsh/worktrees/tvVNC/maestro-large/2026-09-21_001005/large_text/takeScreenshot/private-keyboard-large.png, where body text wraps at roughly twenty-eight to thirty-two characters per line, it will occupy around two and a half lines at the default size and around four at the largest scale. If it must be shortened, cut the first sentence and never the second, because the first describes something the user discovers within one keystroke while the second describes something they cannot discover until it has already happened. The word "above" depends on the keys staying physically below the input field, their position in all three supplied screenshots; a layout that moves them invalidates the word. Nothing has been rendered, so these length judgements come from the existing screenshots rather than from the surface itself.

STRING: pausedEditingBody
KEYS: pausedEditingBody
ENGLISH: "Live edit is paused. Select Live edit to load the current TV field."
VERDICT: REWORD
REWRITE: Live edit is paused, so your text is not being sent. Select Live edit to load the TV's text, replacing what is here.
REASONING: Both assumptions this record rested on are now resolved in favour of the rewrite, and neither resolution requires a word to change. Your first fact confirms that selecting Live edit while paused replaces the retained local buffer with the current television text, and that pausing itself retains the text while reloading does not; that makes the closing clause "replacing what is here" a true statement of a real cost, and it confirms the defect in the submitted string, which instructs the user to take an action that destroys text they typed without telling them so. A warning that directs the user into the loss it should be preventing is worse than no warning, which is why this clause is the one part of the sentence that must never be cut. Your second fact confirms that the Live edit chip is unselected while paused, which removes the one thing that could have made the instruction unfollowable: the user is being told to select a chip that genuinely appears unselected, so "Select Live edit" is an action they can actually take. The same fact confirms that Send is disabled in this state, which is what the clause "so your text is not being sent" exists to explain, since a greyed-out control with no stated cause is the most likely question a user has at this moment and the abstraction "paused" does not by itself connect to it. The exact Compose strings also confirm a choice made here for the same reason as in the live body: the field in the paused state holds the retained local buffer, not the separate Compose draft, so the shipped noun "draft" is deliberately not reused and the neutral phrase "what is here" is used instead.
RISKS: At one hundred and sixteen characters this is a long inline warning, roughly three lines at the default size and around five at the largest scale on the evidence of the large-text capture, and it has never been rendered. Your first fact also establishes that the Compose draft is separate and unaffected by the reload, and the rewrite deliberately does not say so, because the warning is already at the upper bound of what an inline block can carry and a user stopped inside the live mode is not looking at their Compose draft at that moment; the cost of that omission is that the phrase "what is here" must be understood as scoped to the visible field, which context supplies but which should be confirmed on the rendered screen. If length must come down, the clause to lose is "so your text is not being sent", since the disabled Send button is at least visible, and the clause to keep at all costs is "replacing what is here". The repetition of "Live edit" across the two sentences is intentional, the first naming the mode and the second naming the chip the user must operate, and collapsing it would weaken the instruction.

STRING: cursorLeft
KEYS: cursorLeft
ENGLISH: "Move cursor left"
VERDICT: APPROVE
REASONING: The contrast I could not check before is now checked, and it resolves in this string's favour more strongly than I was able to claim. The exact Compose control label is "Move cursor left on TV", and this live-mode label is that same label with the trailing qualifier removed and nothing else altered. That is the cleanest possible relationship between two mode variants: identical action naming, with the television named only when the television is actually reached. It also means this new string is not an invention at all but the existing shipped label minus a qualifier that has stopped being true, which is the strongest consistency argument available and one I could not make while the Compose label was described to me only as "Backspace on TV etc". The premise correction applies here as well: I earlier wrote that this label "carries no leading TV marker" and treated the leading position as the product's convention for controls, which was wrong, since controls mark the television at the end rather than the front. The conclusion is unaffected and in fact reinforced, because under the real convention the correct way to withdraw the television claim from a control is to drop the trailing "on TV", which is exactly what this string does. The label remains verb-led, which suits a control whose name is also spoken as its accessibility name, it describes an observable outcome rather than the underlying mechanism, and it claims nothing about the television's own cursor, which the brief does not say is mirrored.
RISKS: One checkable item remains and it is recorded in the closing note below, concerning the Backspace and Delete labels in the live modes. It bears on this string because the four editing controls must withdraw the television qualifier together; if Backspace and Delete keep a trailing "on TV" while the cursor keys drop it, the row becomes internally inconsistent and a user reading two neighbouring buttons gets two contradictory signals about where their keystroke lands. Because the live-mode label is the Compose label minus a suffix, the two are identical for their first three words, so a user scanning quickly sees the same opening in both modes; that is acceptable, because the protective rule is that the phrase "on TV" is present whenever and only when the television is reached, but it does mean the qualifier must never be truncated or moved by a layout that clips long tooltips. Nothing here has been rendered.

STRING: cursorRight
KEYS: cursorRight
ENGLISH: "Move cursor right"
VERDICT: APPROVE
REASONING: Everything established for the left-hand label holds here without modification, and the confirmed Compose label "Move cursor right on TV" completes the pattern: the two live-mode cursor labels are the two shipped Compose labels with the trailing television qualifier removed, so all four strings form a single consistent set rather than two unrelated pairs. The exact mirror-image construction between left and right is itself the value, because a user who learns one label has learned the other, and a screen-reader user hearing them in sequence gets an unambiguous pair distinguished by a single word in the position where the difference actually lies. For that reason this string must never be reworded independently of its partner.
RISKS: The same open item about the Backspace and Delete labels in the live modes applies equally here, since all four editing controls in the row need to withdraw the television qualifier together to stay coherent. If either cursor label is ever shortened for width, both must be shortened identically and in the same change, because an asymmetric pair costs more in comprehension than either label saves in space. Nothing here has been rendered, so the behaviour of these tooltips against the narrow round buttons visible in the supplied screenshots has not been seen.

RECORD CORRECTION: The earlier records in this batch asserted that the product marks television-scoped items with a leading "TV" and cited "TV Backspace" as a shipped example. That label is not shipped and the citation was wrong. The real convention, from the exact strings supplied, places the marker in front for the section heading, "TV editing keys", and behind for every control, "Backspace on TV", "Delete on TV", "Move cursor left on TV", "Move cursor right on TV" and "Enter on TV". No verdict in this batch changes, because each argument that leaned on the convention was about the correct element type in every case: the heading argument concerned a heading, where the leading form is genuinely used, and the control arguments concerned controls, where dropping the trailing qualifier is the right way to withdraw the television claim. The same misquoted label also appeared in the earlier reasoning for the screen-reader name of the adjustable divider, where I argued against a leading "TV". That argument survives the correction, because the heading "TV editing keys" shows that a leading "TV" does mark a television-scoped item, so the name "TV preview size" would still have read as something acting on the television; the conclusion and the rewrite "Screen height" are unaffected, and those five strings are not re-reviewed here.

BRIEF DEFECT: One item remains open and it is narrow. The previous brief stated that the "Existing Backspace/Delete labels" are "reused unchanged for local text actions" in the live modes, and the exact Compose labels are now confirmed as "Backspace on TV" and "Delete on TV". Read literally together, those two statements would put the words "on TV" on a button that edits the phone's mirrored field, which would be a false statement in a shipped label and would also break the contrast that justifies approving both cursor labels. The reading that makes the batch coherent is that unqualified "Backspace" and "Delete" strings already exist elsewhere in the product and are the ones being reused, which would also explain why only the cursor keys needed new strings in this batch, since no unqualified "Move cursor left" or "Move cursor right" existed to reuse. That reading is an inference and has not been confirmed, and it should be confirmed before these five strings ship, because all four editing controls in the row must withdraw the television qualifier together. Separately, and as disclosed throughout, none of these five strings has been rendered, so every judgement above about line length, wrapping and neighbouring controls is reasoning from the three existing screenshots and must be re-checked on the real screen after implementation.

COPY REVIEW COMPLETE: 5 strings (3 approved, 2 reworded)
